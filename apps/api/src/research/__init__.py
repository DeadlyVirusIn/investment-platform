"""Research-only modules — shadow simulations + replacement candidates.

Nothing here is wired into live trading. Every function is read-only
and produces verdicts/snapshots consumed by reports + Ops UI only.
"""
