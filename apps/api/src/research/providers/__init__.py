"""Phase 11W (Phase D.1) — research provider implementations.

Only the deterministic mock provider exists in Phase D.1. Future
phases may register real-LLM providers behind explicit env flags +
cost ceilings + dry-run defaults; none is wired in this phase.
"""
