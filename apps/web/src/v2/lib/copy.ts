// P1.3 — shared product copy. Single source of truth so the same
// sentence renders identically across surfaces (no drift).

// Rendered under every confidence display (Today hero, Pick page,
// Opportunities). States what the score is — and what it is not.
export const CONFIDENCE_DOCTRINE =
  "Confidence measures how strongly today's signals agree. It is not a prediction.";
